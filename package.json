{
	"name": "org-webhook-spliter",
	"author": {
		"name": "wyf9",
		"email": "wyf9@siiway.top",
		"url": "https://github.com/wyf9"
	},
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"types": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.40.2"
	},
	"cloudflare": {
		"bindings": {
			"SECRET": {
				"description": "访问密钥, 如不为 disabled 则只允许路径为此值的请求 (详见 https://github.com/siiway/org-webhook-spliter#secret) <br/> Access Key (if not `disabled`, will only allow requests made to this path, See also: TODO)"
			},
			"REPO_CONFIG": {
				"description": "基于仓库的配置 (详见 https://github.com/siiway/org-webhook-spliter#repo_config) \n Forwarding config for single repository"
			},
			"ORG_CONFIG": {
				"description": "基于组织的配置 (详见 https://github.com/siiway/org-webhook-spliter#org_config) \n\n Forwarding config for organiztions"
			}
		}
	}
}