{
	"name": "org-webhook-spliter",
	"author": {
		"name": "wyf9",
		"email": "wyf9@siiway.org",
		"url": "https://github.com/wyf9"
	},
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"types": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.71",
		"typescript": "^5.9.3",
		"vitest": "~4.1.0",
		"wrangler": "^4.92.0"
	},
	"cloudflare": {
		"bindings": {
			"SECRET": {
				"description": "访问密钥, 如不为 disabled 则只允许路径为此值的请求 (详见 https://github.com/siiway/org-webhook-spliter#secret) | Access Key (if not `disabled`, will only allow requests made to this path, See also: https://github.com/siiway/org-webhook-spliter/blob/main/README.en.md#secret)"
			},
			"REPO_CONFIG": {
				"description": "基于仓库的配置 (详见 https://github.com/siiway/org-webhook-spliter#repo_config) | Forwarding config for single repository (See also: https://github.com/siiway/org-webhook-spliter/blob/main/README.en.md#repo_config)"
			},
			"ORG_CONFIG": {
				"description": "基于组织的配置 (详见 https://github.com/siiway/org-webhook-spliter#org_config) | Forwarding config for organiztions (See also: https://github.com/siiway/org-webhook-spliter/blob/main/README.en.md#org_config)"
			},
			"HEADERS": {
				"description": "全局请求 Headers 配置 (详见 https://github.com/siiway/org-webhook-spliter#headers) | Global request headers config (See also: https://github.com/siiway/org-webhook-spliter/blob/main/README.en.md#headers)"
			}
		}
	},
	"dependencies": {
		"@types/js-yaml": "^4.0.9",
		"js-yaml": "^4.3.0"
	}
}